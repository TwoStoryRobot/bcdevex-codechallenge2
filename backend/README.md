# Backend

## Environment Variables

```
SMTP=smtp://localhost:1025 # Url of SMTP service
FROM_EMAIL=no-reply@code-challenge.com
CORS_ORIGIN # TODO: elaborate more on this environment variable
CLIENT_ID # TODO: elaborate more on this environment variable
POSTGRES_HOST=localhost # host for Postgres (Defaults to specified)
POSTGRES_POST=5432 # port for Postgres (Defaults to specified)
POSTGRES_DB=code_challenge # Postgres database name (Defaults to specified)
POSTGRES_USER=postgres # username for Postgres (Defaults to specified)
POSTGRES_PASSWORD=postgresadmin # password for Postgres (Defaults to specified)
```
